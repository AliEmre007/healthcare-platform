# Frontend

The frontend is intentionally deferred. The planned stack is React, TypeScript, Vite, Tailwind CSS, React Query, React Hook Form, and Zod.

This directory reserves the future application boundary so the repository can grow into a full-stack portfolio project without mixing frontend and backend concerns.
